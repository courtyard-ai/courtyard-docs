name: Deploy Internal Docs

on:
  push:
    branches:
      - main
    paths:
      - 'docs-internal/**'
      - 'mkdocs-internal.yml'
      - '.github/workflows/deploy-internal.yml'

permissions:
  contents: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: 3.x

      - name: Install dependencies
        run: |
          pip install mkdocs-material

      - name: Deploy to GitHub Pages (internal branch)
        run: |
          mkdocs gh-deploy --config-file mkdocs-internal.yml --force --remote-branch gh-pages-internal
